import React from "react";
export const ListboxWrapper = ({children}) => (
  <div className=" w-min-screen bg-transparent h-min-screen border-small m-0 rounded-small border-default-200 dark:border-default-500">
    {children}
  </div>
);